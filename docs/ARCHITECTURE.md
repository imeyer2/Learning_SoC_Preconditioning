# Complete System Architecture

## End-to-End Pipeline

```
┌─────────────────────────────────────────────────────────────────────────────┐
│                         TRAINING PIPELINE                                   │
└─────────────────────────────────────────────────────────────────────────────┘

1. DATA GENERATION (src/p_sparsity/data/)
   ┌────────────────────────────────────────┐
   │ Problem Generators (Registry Pattern)  │
   │  • AnisotropicDiffusionGenerator       │
   │  • ElasticityGenerator (placeholder)   │
   │  • HelmholtzGenerator (placeholder)    │
   └────────────────────────────────────────┘
              ↓
   Outputs: {A, node_features, edge_index, edge_weight, smooth_errors}

2. MODEL FORWARD PASS (src/p_sparsity/models/)
   ┌────────────────────────────────────────┐
   │ AMGEdgePolicy                          │
   │  ┌──────────────────────────────────┐  │
   │  │ GNN Backbone (swappable)         │  │
   │  │  • GAT (default)                 │  │
   │  │  • GCN                           │  │
   │  │  • GraphSAGE                     │  │
   │  └──────────────────────────────────┘  │
   │              ↓                          │
   │  ┌──────────────────────────────────┐  │
   │  │ Edge Features Module             │  │
   │  │  • Strength features             │  │
   │  │  • Geometric features            │  │
   │  │  • Distance features             │  │
   │  └──────────────────────────────────┘  │
   │              ↓                          │
   │  ┌──────────────────────────────────┐  │
   │  │ Output Heads                     │  │
   │  │  • Edge MLP → logits             │  │
   │  │  • B head → candidates (opt)     │  │
   │  └──────────────────────────────────┘  │
   └────────────────────────────────────────┘
              ↓
   Outputs: (edge_logits, B_extra)

3. SAMPLING (src/p_sparsity/pyamg_interface/sampling.py)
   ┌────────────────────────────────────────┐
   │ sample_topk_without_replacement        │
   │  • Gumbel-softmax trick                │
   │  • Temperature annealing               │
   │  • Differentiable sampling             │
   └────────────────────────────────────────┘
              ↓
   Outputs: (selected_edges, log_prob)

4. SOLVER BUILDING (src/p_sparsity/pyamg_interface/solver_builder.py)
   ┌────────────────────────────────────────┐
   │ C_from_selected_edges                  │
   │  • Build binary C matrix               │
   │  • Ensure symmetry                     │
   └────────────────────────────────────────┘
              ↓
   ┌────────────────────────────────────────┐
   │ build_B_for_pyamg                      │
   │  • Prepend constant vector             │
   │  • Normalize learned vectors           │
   └────────────────────────────────────────┘
              ↓
   ┌────────────────────────────────────────┐
   │ build_pyamg_solver                     │
   │  • PyAMG SA with learned C, B          │
   │  • Build multilevel hierarchy          │
   └────────────────────────────────────────┘
              ↓
   Outputs: PyAMG multilevel solver

5. REWARD COMPUTATION (src/p_sparsity/rl/rewards.py)
   ┌────────────────────────────────────────┐
   │ vcycle_energy_reduction_reward         │
   │  • Run V-cycle on random RHS           │
   │  • Compute energy norm reduction       │
   │  • Add complexity penalty              │
   └────────────────────────────────────────┘
              ↓
   Outputs: reward (higher = better)

6. RL UPDATE (src/p_sparsity/rl/algorithms/reinforce.py)
   ┌────────────────────────────────────────┐
   │ ReinforceTrainer                       │
   │  • Compute advantage w/ baseline       │
   │  • Policy gradient: ∇L = -A * ∇log(π)  │
   │  • Update model parameters             │
   │  • Anneal temperature                  │
   └────────────────────────────────────────┘
              ↓
   Repeat for next epoch

┌─────────────────────────────────────────────────────────────────────────────┐
│                         EVALUATION PIPELINE                                 │
└─────────────────────────────────────────────────────────────────────────────┘

1. LOAD CHECKPOINT
   trained_model.pt → model.eval()

2. BUILD SOLVERS (src/p_sparsity/evaluation/)
   ┌────────────────────────────────────────┐
   │ Learned Solver                         │
   │  • Use model to select edges (C)       │
   │  • Use model B candidates (optional)   │
   │  • Build PyAMG hierarchy               │
   └────────────────────────────────────────┘
              +
   ┌────────────────────────────────────────┐
   │ Baseline Solver                        │
   │  • Standard PyAMG SA                   │
   │  • Default strength measure            │
   └────────────────────────────────────────┘

3. PCG ANALYSIS (src/p_sparsity/evaluation/pcg_analysis.py)
   ┌────────────────────────────────────────┐
   │ run_pcg_analysis                       │
   │  • Run CG with AMG preconditioner      │
   │  • Track residuals per iteration       │
   │  • Compute convergence rate            │
   └────────────────────────────────────────┘
              ↓
   Metrics: iterations, residuals, speedup

4. V-CYCLE ANALYSIS (src/p_sparsity/evaluation/vcycle_analysis.py)
   ┌────────────────────────────────────────┐
   │ run_vcycle_analysis                    │
   │  • Apply V-cycles iteratively          │
   │  • Track error/energy norm reduction   │
   │  • Compute avg reduction factor        │
   └────────────────────────────────────────┘
              ↓
   Metrics: error_norms, energy_norms, reduction_factors

5. EIGENVALUE ANALYSIS (src/p_sparsity/evaluation/eigenvalue_analysis.py)
   ┌────────────────────────────────────────┐
   │ run_eigenvalue_analysis                │
   │  • Compute spectrum of M^{-1}A         │
   │  • Condition number                    │
   │  • Spectral radius                     │
   └────────────────────────────────────────┘
              ↓
   Metrics: eigenvalues, condition_number, spectral_radius

6. VISUALIZATION (src/p_sparsity/visualization/)
   ┌────────────────────────────────────────┐
   │ Generate Plots                         │
   │  • Sparsity patterns (C comparison)    │
   │  • Convergence curves (PCG, V-cycle)   │
   │  • Training progress (loss, reward)    │
   │  • Performance comparison              │
   └────────────────────────────────────────┘
              ↓
   Outputs: PNG/PDF figures + markdown tables

┌─────────────────────────────────────────────────────────────────────────────┐
│                         KEY CONCEPTS                                        │
└─────────────────────────────────────────────────────────────────────────────┘

WHAT THE MODEL LEARNS:
╔═══════════════════════════════════════════════════════════════════════════╗
║  1. Edge Logits → C Matrix (Primary)                                     ║
║     • For each edge (i,j), predict "strength" logit                      ║
║     • Sample top-k edges per row → binary C matrix                       ║
║     • C determines aggregation in PyAMG                                  ║
║                                                                           ║
║  2. B Candidates → Near-Nullspace (Optional)                             ║
║     • Predict k vectors that should be preserved by P                    ║
║     • Help PyAMG build better interpolation operator                     ║
║     • Combined with constant vector: B = [ones, learned_1, learned_2]    ║
╚═══════════════════════════════════════════════════════════════════════════╝

WHAT PyAMG COMPUTES:
╔═══════════════════════════════════════════════════════════════════════════╗
║  From C and B, PyAMG builds:                                             ║
║   • Aggregates (groups of fine nodes → coarse nodes)                     ║
║   • Tentative prolongation P_tent (using B)                              ║
║   • Smoothed prolongation P = S * P_tent                                 ║
║   • Restriction R = P^T                                                  ║
║   • Coarse operator A_c = R * A * P                                      ║
║   • Recursive hierarchy until coarse enough                              ║
╚═══════════════════════════════════════════════════════════════════════════╝

THE LEARNING PROCESS:
┌─────────────────────────────────────────────────────────────────────────────┐
│ Policy: π(C, B | A, features)                                              │
│    ↓                                                                        │
│ Sample: C ~ Gumbel-Softmax(logits, τ)                                      │
│    ↓                                                                        │
│ Build: ml = PyAMG(A, C=C, B=B)                                              │
│    ↓                                                                        │
│ Test: Apply V-cycle, measure energy reduction                               │
│    ↓                                                                        │
│ Reward: R = -log(energy_after / energy_before) - λ * sparsity              │
│    ↓                                                                        │
│ Update: ∇θ J ≈ (R - baseline) * ∇θ log π(C, B | A)                         │
│    ↓                                                                        │
│ Improve: Better C and B → Better P → Faster convergence                    │
└─────────────────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────────────────┐
│                         MODULE DEPENDENCIES                                 │
└─────────────────────────────────────────────────────────────────────────────┘

data ──────────┐
               ├──→ models ──→ pyamg_interface ──→ rl ──→ train.py
utils ─────────┘                     │
                                     │
                                     └──→ evaluation ──→ evaluate.py
                                            │
                                            ↓
                                      visualization

CLEAN SEPARATION:
✅ data:          Knows nothing about ML or PyAMG
✅ models:        Knows nothing about PyAMG or training
✅ pyamg_interface: Knows nothing about training
✅ rl:            Knows nothing about problem generation
✅ evaluation:    Knows nothing about training
✅ visualization: Knows nothing about computation

This enables:
• Easy testing of individual components
• Swapping implementations without touching other modules
• Extending via registry patterns
• Configuration-driven behavior changes

┌─────────────────────────────────────────────────────────────────────────────┐
│                         CONFIGURATION HIERARCHY                             │
└─────────────────────────────────────────────────────────────────────────────┘

configs/
├── model/gat_default.yaml
│   ├── backbone: gat              ← Swap to 'gcn' or 'graphsage'
│   ├── hidden_dim: 64             ← Change architecture size
│   ├── learn_B: true              ← Enable/disable B learning
│   └── B_extra: 2                 ← Number of learned B vectors
│
├── training/reinforce_default.yaml
│   ├── epochs: 100                ← Training duration
│   ├── learning_rate: 1e-3        ← Optimizer config
│   ├── reward: vcycle_energy_reduction  ← Swap reward function
│   └── baseline: moving_average   ← Variance reduction
│
├── data/anisotropic_default.yaml
│   ├── type: anisotropic          ← Swap to 'elasticity', etc.
│   ├── grid_size: 50              ← Problem size
│   └── anisotropy_ratio: 1000     ← PDE parameters
│
└── evaluation/default.yaml
    ├── run_pcg: true              ← Enable/disable metrics
    ├── run_vcycle: true
    └── test_cases: [...]          ← Which problems to test

All modular! Change configs, not code. 🎉
